// 05_logger.gm: Demonstrates append_file and cat
var log_path = "samples/files/app.log";

append_file(log_path, "Log Entry: Program started\n");
append_file(log_path, "Log Entry: Processing data...\n");
append_file(log_path, "Log Entry: Task complete\n");

println("Current Log Contents:");
cat(log_path);

// Clean up for next run by truncating
// truncate_file(log_path, 0);
// println("\nLog truncated to 0 bytes.");